# BACKLOG - ebpfsieve

src/program.rs:126 | bug | medium | The counts histogram array uses u16 elements which can saturate if window_size exceeds u16::MAX (65535), leading to desynchronized counts and false negatives when sliding the window. | Change counts element type to usize or u32 to support arbitrary window sizes up to the window_size limit. | status=done (changed counts to usize in matches_bytes and matching_windows; added regression test u16_counts_do_not_saturate_in_large_windows)
src/iter.rs:29 | bug | medium | The MatchWindowIter counts histogram array uses u16 elements which can saturate if window_size exceeds u16::MAX (65535), leading to desynchronized counts and false negatives when sliding the window. | Change counts element type to usize or u32 to support arbitrary window sizes up to the window_size limit. | status=done (changed counts to usize in MatchWindowIter; covered by u16_counts_do_not_saturate_in_large_windows regression test)
src/bpf/sieve.bpf.c:26 | stub | high | The sieve_vfs_read function is a stub returning 0 without doing frequency counting or threshold checking, rendering the kernel eBPF offload a silent no-op. | Implemented as `fexit/vfs_read` (so the user buffer is valid after `vfs_read` writes). For reads up to 128 bytes it copies the returned bytes with `bpf_probe_read_user`, counts byte frequencies on the eBPF stack, checks the thresholds map, and emits a `SkipDecision` to the `rb` ring buffer when the read cannot satisfy all thresholds. For reads larger than 128 bytes it skips the whole read if the actual returned length is smaller than a required threshold (so no window can possibly meet). | status=done (C program compiles; rb map type corrected to BPF_MAP_TYPE_RINGBUF; cargo test -p ebpfsieve --features kernel-bpf passes 201; clippy clean)
src/kernel.rs:246 | bug | high | byte_frequency_filter_to_literal_pattern translates non-contiguous frequency thresholds into a contiguous literal pattern, causing the JIT-compiled socket BPF filter to drop packets that contain non-contiguous matches and causing false negatives. | Replaced compile_socket_filter_program with compile_frequency_count_socket_filter: a custom eBPF program that counts byte frequencies across the whole packet using a 256-entry u16 table on the stack, then checks each threshold. | status=done (frequency-counting socket filter compiles; cargo test -p ebpfsieve --features socket-bpf passes 219; updated stale literal-pattern tests to assert rejection or frequency compilation)
src/kernel.rs:474 | stub | medium | drain_ring_buffer unconditionally deactivates the kernel filter when polled, disabling the eBPF offload path upon the first call to poll_skips. | drain_ring_buffer was already implemented: it polls the aya RingBuf and drains test_injected_skips without changing active. | status=done (drain_ring_buffer_without_rb_keeps_filter_active test confirms filter stays active after draining; cargo test -p ebpfsieve --features kernel-bpf --lib passes)
src/program.rs:319 | silent-fallback | low | from_toml_file maps file I/O read errors to Error::ReadFailed, which has a misleading fix message advising to verify the reader remains open and readable. | Return Error::InvalidConfiguration or a new specific configuration-read error type to guide the user on fixing config file issues. | status=done (added Error::ConfigurationReadFailed with path/permissions guidance; used in from_toml_file)
src/loader.rs:130 | silent-fallback | low | read_next constructs a completely empty FilteredChunk on read error, discarding carry and failing to return any partial results as claimed in comments. | Update the error branch in read_next to populate FilteredChunk with any carry data and matches before returning, or clean up the comments and API to reflect that no partial chunk is returned. | status=done (error branch now returns the carried overlap as a partial chunk; added regression test read_next_returns_carry_on_error)
DONE 2026-07-14 src/loader.rs:60 | capability | high | FileReadFilter has no integration with KernelFilter, meaning userspace scanning never polls or utilizes the eBPF skip decisions to skip file reads. | Implemented: added `Option<KernelFilter>` and an `Option<fn(&mut R, SeekFrom)>` seek function pointer to `FileReadFilter`; `new_seekable` sets the seek function; `set_kernel_filter` attaches a filter; `read_next` polls `poll_skips` and uses the seek function to advance the reader past each skipped region. Non-seekable readers ignore skip decisions rather than silently degrading. Test: `loader::tests::kernel_filter_skip_decisions_move_reader_past_skipped_region`. `cargo test -p ebpfsieve` passes 200. | status=done
src/program.rs:248 | silent-fallback | medium | scan_file reads a full chunk before checking if the max_bytes limit is exceeded, which can block indefinitely on slow streams like pipes or sockets even when the limit is already satisfied. | Dynamically adjust the read chunk size in read_next based on the remaining max_bytes budget to avoid over-reading. | status=done (added FileReadFilter::set_next_read_limit and clamped scan_file reads; added regression test next_read_limit_clamps_final_chunk_size)
src/loader.rs:155 | dead-code | low | The filter checking if range.offset + range.length > carry_len is redundant and always true because carry_len is always strictly less than window_size. | Remove the redundant filter block to simplify the code and avoid unnecessary comparisons. | status=done (removed redundant .filter block; existing tests still pass)
src/program.rs:157 | perf | medium | matching_windows initializes and constructs a 256-element array of Vec on every call, creating unnecessary allocation overhead in the hot path of chunk scanning. | Precompute the byte_to_thresholds map at filter construction time and store it inside the ByteFrequencyFilter struct. | status=done (added byte_to_threshold_indices Vec<Vec<usize>> computed in new(); all matching_windows paths use it; serde roundtrip preserved)
src/loader.rs:122 | bug | low | read_next resizes the read buffer to chunk_size without validation, allowing a chunk_size of usize::MAX to cause an Out of Memory panic. | Add an upper limit validation to chunk_size in with_chunk_size and config parsing to reject absurdly large values. | status=done (with_chunk_size now rejects chunk_size > isize::MAX; updated test_15_chunk_size_usize_max to assert rejection)
tests/comprehensive_tests.rs:806 | modernization | low | Stale comments and test workarounds claiming MatchWindowIter next() calls panic due to a bug in iter.rs line 56. | Remove the stale comments and update the test cases to fully assert subsequent match windows returned by the iterator. | status=done (updated iterator_lazy_evaluation to assert all match windows and compare with matching_windows)
tests/comprehensive_tests.rs:828 | modernization | low | Stale comment claiming MatchWindowIter has an overflow bug when data is shorter than window. | Remove the stale comment and implement proper test assertions verifying the iterator behaves correctly on short data. | status=done (updated iterator_data_shorter_than_window to test the iterator itself and compare with matching_windows)

- [2026-07-14 publish-prep] performance/kernel/ebpfsieve | patch bump 0.1.0->0.1.1 (published max 0.1.0); no code change; self-contained.
src/kernel.rs:662 | ebpfsieve | silent-fallback | medium | REPOPULATE 2026-07-16: RingBuf::try_from fails to open the eBPF ring buffer, Option::None is substituted via ok() discarding the error, and active is still set to true, causing the kernel filter to silently run without ever delivering skip decisions to userspace | Fail loudly by propagating the error from RingBuf::try_from during try_attach, and add a unit test using an invalid map type to prove that initialization fails with an expected Ebpf error | status=done | FIXED (Law 10 fail-closed): replaced `bpf.take_map("rb").and_then(|map| RingBuf::try_from(map).ok())` - which swallowed a present-but-unopenable ring buffer into None while `active: true`, so the kernel program ran and dropped every skip decision with zero operator signal - with a new generic helper `open_ring_buffer(map, open)` that propagates the open error as a hard `Error::InvalidConfiguration` (aborting attach) when the map is present, and yields None only when the map is genuinely absent. The call site passes `aya::maps::RingBuf::try_from`. The helper is generic over map/ringbuf types + the opening op so the error branch is unit-testable WITHOUT a live kernel (the real "invalid map type" boundary is `#[cfg(all(target_os=linux, feature=kernel-bpf))]` and needs root+BTF+a malformed BPF object, so it is not a plain unit test). Proving tests (kernel::tests): open_ring_buffer_fails_closed_when_present_map_cannot_open (Display-erroring open -> Err::InvalidConfiguration naming the 'rb' map + carrying the failure + fix mentioning BPF_MAP_TYPE_RINGBUF), open_ring_buffer_absent_map_yields_none_not_error (None -> Ok(None), open never called), open_ring_buffer_present_and_openable_yields_some (happy path -> Some). `cargo test -p ebpfsieve --lib` = 28 passed, 0 failed, EXIT=0.
src/loader.rs:172 | ebpfsieve | performance | medium | REPOPULATE 2026-07-16: read_next unconditionally calls poll_skips and to_vec on the active kernel filter, allocating a fresh heap vector on every single chunk read even when the reader is non-seekable or there are no skips | Poll the kernel filter only when seek_fn is Some, and iterate over the returned slice directly to eliminate all heap allocations in the hot loop, verified by a proving unit test showing allocation-free skips | status=done | FIXED (Law 7 hot-path alloc): the old `let skips: Vec<SkipDecision> = kernel_filter.map_or_else(Vec::new, |kf| kf.poll_skips().to_vec());` ran on EVERY chunk read - it polled unconditionally (even for a non-seekable reader where skips are unusable) and `.to_vec()`-copied the borrowed slice into a fresh heap Vec even on the overwhelmingly common empty/no-skip path. Restructured so the poll happens ONLY inside `if let Some(seek_fn) = self.seek_fn` (non-seekable readers never poll - Law 10, no silent degrade AND no wasted work) and iterates `kf.poll_skips()` (the borrowed `&[SkipDecision]`) in place with no intermediate Vec; the disjoint seek/carry/offset field borrows keep it sound. Removed the now-unused top-level `SkipDecision` import. Proving test loader::tests::non_seekable_reader_ignores_skip_decisions_and_reads_from_start: same [3,7) skip decision as the seekable test but on `attach` (seek_fn None) - asserts the reader starts at offset 0 and reads the "skippable" region (chunk.data == b"aaaSKIPa"), pinning the poll-only-when-seekable contract; the pre-existing kernel_filter_skip_decisions_move_reader_past_skipped_region still proves the seekable slice-iteration path. `cargo test -p ebpfsieve --lib` = 28 passed, 0 failed, EXIT=0.
src/loader.rs:178-204 + :245-266 | ebpfsieve | correctness | high | Claude 2026-07-17: after a kernel-filter skip decision seeks the reader FORWARD past a no-match region (183-184 sets self.next_offset = new_pos = skip_end), the stale self.carry is NEVER cleared. | On a successful kernel skip-seek that advances next_offset, CLEAR the carry (self.carry.clear()) before the read. | status=done | FIXED: cleared self.carry on successful kernel skip-seek (self.carry.clear()), preventing false matches across file discontinuities and ensuring correct chunk offset calculation after seeking.
src/loader.rs:282-320 (+ skip path 178-204) | ebpfsieve | testing | medium | Claude 2026-07-17: loader tests cover new_bytes accounting, but NO test exercises a kernel SKIP decision with a non-empty carry. | Add a differential test for kernel skip decision with non-empty carry. | status=done | FIXED: added proving test loader::tests::kernel_filter_skip_clears_carry_and_prevents_discontinuity_matches asserting chunk offsets and candidate matches after a kernel filter skip with non-empty carry.
src/loader.rs:132-138 | ebpfsieve | silent-fallback | low | Claude 2026-07-17: set_kernel_filter stores the KernelFilter unconditionally, ignoring non-seekable readers silently. | Make non-seekable case loud. | status=done | FIXED: set_kernel_filter now returns Result<(), Error> and fails loudly with Error::InvalidConfiguration if called on a non-seekable reader. Verified by loader::tests::non_seekable_reader_rejects_kernel_filter_attachment.
src/iter.rs:80-98 (vs src/program.rs:163-176) | ebpfsieve | performance,one-place | medium | Claude 2026-07-17: MatchWindowIter::next re-tests all K thresholds every position (O(N*K)). | Upgrade MatchWindowIter to incremental O(1) satisfied counter. | status=done | FIXED: upgraded MatchWindowIter to track satisfied threshold count and threshold_met bitset incrementally in O(1) amortized time, matching ByteFrequencyFilter::matching_windows. Verified by iter::tests::iterator_and_matching_windows_are_identical.
src/program.rs:170 | ebpfsieve | performance | low | Claude 2026-07-17: matching_windows allocates a fresh vec![false; total_thresholds] on EVERY call. | Replace heap vec with stack bitmask. | status=done | FIXED: replaced per-call heap vec![false; total_thresholds] in matching_windows and MatchWindowIter with stack-allocated ThresholdBitset ([u64; 4]), eliminating all heap allocations for up to 256 thresholds.
